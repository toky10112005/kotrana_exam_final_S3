I.Integration du template dans FlightMVC
    Vue:
        -transférer forms.php(C'est la page Login)   OK

    Configuration:
        -Dans route.php,Configuré le chmin URL "/" pour afficher directement "forms.php" OK
        
II.Login:
    Configuration:
        -Configurer le formulaire de login dans forms.php  (ajout de name dans les 
        champs et configuration du route..juste prendre le champs nom
        et oublier le reste)   OK
        -Créer un model d'user dans models:"UserModel.php"    OK
        -Créer un Controller dans controller:"UserController.php"   OK
        -Dans route récupérée les valeurs des champs du formulaire de login En cours

    Base de donnnées:
        -Créer une base de données "template";(Postgres).   OK
        -Créer la table users(id,Nom) car auto-login OK

    Fonction:
        -Créer une fonction pour Insérer un user dans la table user OK
        -Créer une fonction pour Checker si un user éxiste dans la base:
            "CheckUser($username)"  OK


Prendre message.html dans template et l'integrée dans FlightMVC OK

Configurér Route avec l'URL "/Message" pour rediriger vers message.php ..Aorès le login on redirige 
    l'user vers messages.php    OK 


NE PAS OUBLIER D'INSTALLER LES DRIVER POUR PERMETTRE
UNE CONNEXION À POSTGRES
(COMMANDE:sudo apt install php-pgsql) 

    Ne pas oublier de configurér FlughtMVC pour lire la Base de donnés  OK

III.Messagerie:
    Vue:
        -Intégrer message.html en message.php
        (Utiliser les fonctionnalité du template)   OK
        -

    Base de données:
        -Créer la table "Conversation" (id, user_id, id_destinataire, timestamp) OK
        -Créer la table "Message" (id,contenue,id_conversation, timestamp)  EN cours

    Fonction:
        -Créer un fonction pour insere une Conversation dans la table Conversation:
            (Garder en tête que losque un même paire est sur le point d'être enregistrer 
            on ne change que le timestamp)

        -Créer une fonction pour insérer un message dans la table Message
        -Créer une fonction pour récuperer les messages d'une conversation
        -Créer une fonction pour récuperer les conversations d'un user
         



